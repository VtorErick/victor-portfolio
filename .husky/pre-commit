#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

# Run lint-staged (lints only modified files)
npx lint-staged

# Run TypeScript type check
echo "Running TypeScript type check..."
npx tsc --noEmit

echo "✓ Pre-commit checks passed"
